<%=@comment.errors.full_messages.first if @comment.errors.any?%>
<%= form_for([@tweet, @comment]) do |f| %>
  <div class="field">
    <%= f.label :post %>
    <%= f.text_area :post %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
